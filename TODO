
proc/variable/command completion
 - use MelProjectFiles::get and scan each one for proc and variable defs.
 - build a symbol table and extend HippieProposalProcessor to use it
 - add in the contents of commandList file

integrated quick-help  

browse functions from all buffers
 - could do this in a separate view
 - could integrate into project explorer like java/pydev does -- but how?
 	- need to add IResource children to each IFile discovered by MelProjectFiles::get

each scanner should be preference change listener

code folding

templates

custom search tab - search procs, variables, ctags file ?
 - use token list generated by scanner

 Flag Completion